rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Allow authenticated users (including anonymous users) to access all data
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}
